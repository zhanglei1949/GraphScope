# This can be co-generated with the so
name: "query1"
description: "Procedure query1"
mode: READ  # WRITE, SCHEMA
extension: ".so"
library: libquery1.so  # To specify the directory to the stored procedure
params:
  - name: "mediumTypes"  # TBD
    type: "array(string)"  # 
  - name: "mediumId" # TBD
    type: "string"
returns:
  - name: "paths"
    type: "array(string, double, string)"

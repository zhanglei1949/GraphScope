# This can be co-generated with the so
name: "query0"
description: "Procedure query0"
mode: READ  # WRITE, SCHEMA
extension: ".so"
library: libquery0.so  # To specify the directory to the stored procedure
params:
  - name: "personId2"  # TBD
    type: "long"  # 
  - name: "maxDate" # TBD
    type: "long"
returns:
  - name: "name"
    type: "string"
